import { Button } from "@chakra-ui/react";

const SignInButton = () => {
  return (
    <Button
    variant={"ghost"}
      size={"lg"}
      fontWeight="bold"
    >
      Sign In
    </Button>
  );
};

export default SignInButton;
